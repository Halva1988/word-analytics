import BackgroundImage from "./BackgroundImage";
import TitleHeader from "./TitleHeader";

const Header = () => {
	return (
		<header>
			<BackgroundImage />
			<TitleHeader />
		</header>
	);
};

export default Header;
